const config = {
  // 启动端口
  port: 3030,
  // 数据库配置
  database: {
    DATABASE: 'myBlogSql',
    USERNAME: 'root',
    PASSWORD: '',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config